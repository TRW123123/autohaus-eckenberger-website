---
import { getCars } from "../lib/inventory";
import VehicleCard from "./VehicleCard.astro";

const cars = getCars();
---

<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
    {cars.map((car) => <VehicleCard car={car} />)}
</div>
